const response = await fetch('/api/analisar', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' }, // Essencial para o servidor ler o JSON
    body: JSON.stringify({ prompt: text })
});
